
(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['async', 'buffer', 'eventemitter2'], factory);
    } else {
        var _CoFS = root.CoFS;
        root.CoFS = factory(root.async, root.buffer, root.EventEmitter2);
        root.CoFS.noConflict = function () {
            root.CoFS = _CoFS;
            return CoFS;
        };
    }
}(this, function (async, buffer, eventemitter2) { 
